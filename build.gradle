group 'com.miron'
version '1.0.0'

apply plugin: 'java'
apply plugin: 'idea'
apply plugin: 'org.springframework.boot'

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath("org.springframework.boot:spring-boot-gradle-plugin:1.4.3.RELEASE")
    }
}

jar {
    manifest {
        attributes 'Main-Class': 'com.miron.hackathon1.Application'
    }
}

repositories {
    mavenCentral()
}

dependencies {
    compile("org.springframework.boot:spring-boot-starter-web")
    compile files("libs/aws-java-sdk-1.11.83.jar")
    testCompile("org.springframework.boot:spring-boot-starter-test")
}
